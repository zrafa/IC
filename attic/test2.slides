<!-- INCLUDE src/reveal.header -->

<section>
<h2>Vacía</h2>
</section>


<section>
<h2>Snap test</h2>
<span class="fragment" id="frag1"></span>
<div id="prueba" class="snapsvg" width="100%" height="100%">
<svg viewBox="0 0 400 300" width="100%" height="100%"></svg>
</div>
<!--
{
	"initialize" : "function(container) {
		var svg = Snap('#prueba svg'); 
		var rk = svg.rect(10,10,10,10,3).attr({fill: '#fab'});

	}"
}
-->
</section>


<!--
		var Byte = function(x, y, v) { 
			for (var i = 0; i < 8; i++) { 
				var r = svg.rect(x + i * 10, y, 10, 10).attr({ 
					fill: '#fff', stroke: '#000', strokeWidth: '0.5', 
				}); 
				var t = svg.text(x + 10 * i + 5, y + 5, v[i]).attr({ 
					fontSize: '7', 
					fontFamily: 'Helvetica', 
					textAnchor: 'middle', 
					dominantBaseline: 'middle', 
					alignmentBaseline: 'middle', 
				}); 
			} 
		};
		for (var j = 0; j < 5; j++) { 
			Byte(20, 20 + 10 * j, '00100110'); 
		};
		var zoomin1 = function() { svg.animate({viewBox: g[3].getBBox().vb},800); }; 
		var zoomout1 = function() { svg.animate({viewBox: '0 0 400 300'},800); };

		var shown = { 'frag1' : zoomin1, };
		var hidden = { 'frag1' : zoomout1, };

		Reveal.addEventListener('fragmentshown',function(event){ if(typeof shown[event.fragment.id] === 'function') { shown[event.fragment.id](); } });
		Reveal.addEventListener('fragmenthidden',function(event){if(typeof hidden[event.fragment.id] === 'function') {hidden[event.fragment.id]()};});

-->
<!-- INCLUDE src/reveal.trailer -->
